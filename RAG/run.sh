#!/bin/bash

# For dependencies, main.py file reads requirements.txt 
pip install -r requirements.txt

# Runs the Python module in the main.py
python3 main.py

# For configurations, main.py file reads config.py
